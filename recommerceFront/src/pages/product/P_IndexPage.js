import React from "react";
import { Outlet } from "react-router-dom";

const P_IndexPage = () => {
  return (
    <div>
      P_IndexPage
      <Outlet />
    </div>
  );
};

export default P_IndexPage;
